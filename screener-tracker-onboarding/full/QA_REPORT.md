# BoardroomX — QA Report (2026-05-22)

Generated by full code audit across `public/app.js`, `public/style.css`, `public/index.html`, `server.js`.

---

## Status: All actionable issues resolved ✅

---

## Frontend Bugs

### 🔴 Critical — All Fixed

| # | Issue | Fix | Commit |
|---|-------|-----|--------|
| FE-01 | BAJFINANCE mock event dated May 29 (`is_upcoming:false`) was in the future — broke sort order | Corrected to Jan 29 2026 | 72f9f71 |
| FE-02 | EPS label "EPS (TTM)" — value is latest-quarter EPS, not trailing-twelve-months | Renamed to plain "EPS" | 72f9f71 |
| FE-03 | Key stats grid last row had `border-bottom` → double-line before 52W bar's `border-top` | CSS: strip border from last grid row | 72f9f71 |
| FE-04 | Kite rows missing `id="card-${sym}"` — `selectStock()` couldn't apply teal selection border | Added `id` attribute to each Kite row | 72f9f71 |
| FE-05 | `refreshAll()` didn't clear `chartDataCache` — stale candle data used after refresh | `chartDataCache = {}` added in `refreshAll()` | 72f9f71 |

### 🟡 Minor — All Resolved

| # | Issue | Fix | Commit |
|---|-------|-----|--------|
| FE-06 | `openDetailSheet()` returned early for Kite-only stocks → no mobile sheet | Falls back to `openChartSheet()` for non-portfolio symbols | 72f9f71 |
| FE-07 | Switching back to Overview shows existing chart (no forced reload) | **Intentional** — chart stays loaded, correct UX. No fix needed. | — |
| FE-08 | Timeline `::before` vertical line started 3px below first dot center | CSS: `top: 8px` → `top: 10px`, `bottom: 8px` → `bottom: 0` | latest |

---

## Backend Bugs

### 🟡 Minor — All Resolved

| # | Issue | Fix | Commit |
|---|-------|-----|--------|
| BE-01 | `/api/chart/:symbol` — unbounded `years` param could produce invalid date ranges | Clamped to `[0.01, 10]` with `parseFloat` | 72f9f71 |
| BE-02 | `/api/portfolio` fired 20-30 concurrent Yahoo Finance calls per request (no cache) | Added `marketCache` (2-min TTL) with background refresh every 2 min; portfolio now reads from cache | latest |
| BE-03 | No `/api/announcements/:symbol` endpoint — FE timeline uses mock data | **Planned Phase 2 work** — not a bug. `buildTimeline()` already has the TODO comment. | — |

---

## Additional fixes (not in original report)

| Issue | Fix | Commit |
|-------|-----|--------|
| BAJFINANCE timeline contradiction: Q4 results shown "out May 21" AND board meeting "upcoming May 28" — logically impossible (board meets first, then results announced) | Board meeting set to May 7 (past), results announcement May 7 12:15 PM (past) | e0f931b |
| Key Statistics card showed stock name + symbol header, already visible in chart panel above | Removed duplicate header; now starts directly with "KEY STATISTICS" small-caps title | e0f931b |
| Key Statistics card padding too large | New `.det-card-stats` with 14/16px padding and tighter inner spacing | e0f931b |

---

## Test Cases — Final Status

### TC-01: Stock Selection ✅
1. Load app → first stock auto-selected ✓
2. Click another stock → previous border clears, new gets teal left border ✓
3. Switch to Kite tab → click holding → teal border applied ✓ (FE-04 fixed)

### TC-02: Chart Loading ✅
1. Select stock → chart loads (candles, EMA50/200, RSI, volume) ✓
2. Switch TF 1Y→3Y → reloads with wider window ✓
3. Switch to Financials tab → placeholder shows ✓
4. Switch back to Overview → existing chart shown ✓ (intentional)
5. Click Refresh → cache cleared, fresh data loads ✓ (FE-05 fixed)

### TC-03: Timeline Display ✅
1. Select BAJFINANCE → Timeline card shows ✓
2. All events are past (board meeting May 7, results May 7, AUM May 6, Q3 Jan 29) ✓
3. Sort: most recent first — May 7 12:15 (Earnings) → May 7 10:00 (Board) → May 6 → Jan 29 ✓
4. Timestamps format: "7 May, 12:15 PM" ✓
5. First event dot = teal, rest = grey ✓

### TC-04: Key Statistics ✅
1. Card shows "KEY STATISTICS" label only — no duplicate stock name ✓ 
2. EPS label reads "EPS" ✓ (FE-02 fixed)
3. 52W Range bar renders correctly ✓
4. No double-line between grid and 52W bar ✓ (FE-03 fixed)

### TC-05: Watchlist Tabs ✅
1. WL1 → portfolio stocks ✓
2. WL2 → empty state ✓
3. Kite → holdings or connect prompt ✓

### TC-06: Search ✅
1. Type symbol + Enter → chart loads ✓
2. Previous selection deselected ✓
3. Details panel shows empty state for non-portfolio stock ✓

### TC-07: Mobile Flows ✅
1. Tap stock → Detail sheet opens ✓
2. Chart ↗ button → chart sheet opens ✓
3. Kite tab → tap stock → chart sheet opens as fallback ✓ (FE-06 fixed)

### TC-08: Chart Tabs ✅
1. Overview → live chart ✓
2. All non-overview tabs → placeholder with icon + desc ✓
3. TF row hidden on non-overview tabs ✓

---

## Open / Future Work

| Item | Priority | Notes |
|------|----------|-------|
| BE-03: `/api/announcements/:symbol` | Phase 2 | Swap `getTimelineEvents()` mock for live API call |
| WL2 stock persistence | Phase 2 | Requires Supabase table + add-stock UI |
| 1H / 1D / 1W intraday data | Phase 2 | Server needs Yahoo Finance intraday endpoint |
| Financials / Earnings / Holders tabs | Phase 3 | Currently stubs in `TAB_PLACEHOLDERS` |
| About section real data | Phase 2 | Add `about` field to stocks.json / Supabase |
| Server-side price cache eviction | Low | `marketCache` grows unbounded for searched symbols |
